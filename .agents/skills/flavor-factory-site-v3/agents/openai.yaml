interface:
  display_name: "Flavor Factory Site V3"
  short_description: "Repo-specific patterns and workflows for flavor-factory-site-v3"
  default_prompt: "Use the flavor-factory-site-v3 repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true